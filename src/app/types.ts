export type ProductType = {
    name:string,
    company:string,
    price:number,
    rating:number,
    description:string,
}

export type UserType = {
    username:String,
    email:String,
    id:String,
    isAdmin:boolean,
}